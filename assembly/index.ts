// The entry file of your WebAssembly module.

export function add(a: i32): i32 {

  for (let index = 0; index < a; index++) {   
    
  }

  return a;
}

export function dop(a: i32): i32 {
  return a * a;
}
